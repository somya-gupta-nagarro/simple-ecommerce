
<!doctype html>
<html>
<head>
  <meta charset="utf-8"/>
  <title>Simple Shop</title>
  <link rel="stylesheet" href="/static/styles.css"/>
</head>
<body>
<header>
  <h1>Simple Shop</h1>
  <nav><a href="/cart">Cart</a></nav>
</header>
<main>
  <div class="grid">
    <% products.forEach(p => { %>
      <div class="card">
        <img src="<%= p.image %>" alt="<%= p.name %>"/>
        <h3><%= p.name %></h3>
        <p>₹<%= p.price %></p>
        <button onclick="add(<%= p.id %>)">Add to Cart</button>
      </div>
    <% }) %>
    <button onclick="placeOrder()">Place Order</button>
  </div>
</main>
<script>
async function add(id){
  const res = await fetch('/cart/add', {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({id})});
  const data = await res.json();
  alert('Added! Cart items: ' + data.cartCount);
}
async function placeOrder(){
/*   const res = await fetch('/order/place', { method: 'POST' });
  const data = await res.json();

  if(data.ok){
    alert("Order placed!");
    window.location.href = "/";
  } else {
    alert(data.error);
  } */
  try {
          const response = await fetch("YOUR_LOGIC_APP_URL", {
              method: "POST",
              headers: {
                  "Content-Type": "application/json"
              },
              body: JSON.stringify({
                  name: "Somya",
                  orderId: 123,
                  message: "Order placed successfully"
              })
          });

          const result = await response.text();
          alert("Email triggered successfully!");
      } catch (err) {
          console.error(err);
          alert("Error calling Logic App");
      }
}
</script>
</body>
</html>
